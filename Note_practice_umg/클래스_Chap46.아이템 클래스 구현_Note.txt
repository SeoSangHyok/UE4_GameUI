1. UObject와 AActor의 차이는 레벨에 배치되고 안되고의 여부가 가장 크다. (AActor : 가능, UObject : 불가) 그러므로 UObject는 특정 엑터나 객체의 정보를 처리하는데 좋다.
   즉. 레벨에 먼가 배치해야할것듯 : AActor, AActor의 데이터나 로직처리를 위해선 UObject를 사용하는것이 포인트

2. UObject의 경우 BP생성을 하면 레벨에 배치되는것이 아니기때문에 뷰포트화면이 나오지 않는점 참고

3. BP나 네이티브나 void가 아닌이상 모든 제어경로에서 값을 반환시켜야한다.
